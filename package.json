{
  "name": "evt_parse",
  "version": "1.0.0",
  "description": "compile Equity Variables Test",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/google-apps-script": "^1.0.47"
  },
  "devDependencies": {
    "gts": "^3.1.0",
    "typescript": "^4.0.3"
  }
}
